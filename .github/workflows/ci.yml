name: verified-build

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: verified-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - run: node scripts/verify-phase-0.mjs

  runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
      - run: uv sync --all-packages --dev --frozen
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run mypy
      - run: uv run pytest
      - run: node scripts/verify-phase-2.mjs --ci
      - run: node scripts/verify-phase-3.mjs --ci
      - run: node scripts/verify-phase-4.mjs --ci
      - run: node scripts/verify-phase-5.mjs --ci
      - run: node scripts/verify-phase-6.mjs --ci
      - run: node scripts/verify-phase-7.mjs --ci
      - run: node scripts/verify-phase-8.mjs --ci
      - run: node scripts/verify-phase-10.mjs --ci
      - run: node scripts/verify-phase-11.mjs --ci

  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.8.0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm build
      - run: node scripts/verify-phase-9.mjs --ci

  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infra/terraform
    steps:
      - uses: actions/checkout@v7
      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: 1.13.3
      - run: terraform fmt -check -recursive
      - run: terraform init -backend=false
      - run: terraform validate

  containers:
    runs-on: ubuntu-latest
    needs: [runtime, web]
    steps:
      - uses: actions/checkout@v7
      - run: docker build -f services/runtime/Dockerfile -t veritas-runtime:ci .
      - run: docker build -f apps/web/Dockerfile -t veritas-web:ci .
